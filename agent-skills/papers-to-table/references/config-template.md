# Config template reference

Canonical template path: `app/config.example.json`.

Typical local working copy: `app/config.json`.

Minimum required fields:

- `table_path`
- `schema_path`
- `pdf_dir`
- `output_dir`
- `provider.token`
- `provider.text_model.model_id`
