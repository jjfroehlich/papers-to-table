# Headless usage reference

Recommended baseline command:

```bash
python scripts/papers_to_table.py headless --config app/config.json --accept-all --export
```

Use path overrides only when the config cannot be edited safely in-session.

Always run preflight first when environment readiness is uncertain.
