Field: $column_name
Schema guidance (not evidence unless it appears in the paper): $column_description

Passages:
$context_passages

Return the single best verbatim quote that most directly supports or verifies the proposed field value.
