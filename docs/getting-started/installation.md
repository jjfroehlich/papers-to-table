# Installation

From the repo root:

```bash
python scripts/papers_to_table.py install
```

This installs backend, frontend, eval, and optimizer dependencies.

## Docs tooling (optional)

```bash
python -m pip install -r requirements-docs.txt
```

Use docs tooling only when serving/building the local static manual.
